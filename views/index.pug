doctype html
html
    head
        title #{title}
        link(rel='stylesheet' href='/css/style.css')
        meta(name="viewport" content="width=device-width, initial-scale=1")
    body


        article.container
            each e in animals
                 div  #{e.razza_animale}

